<?xml version="1.0" encoding="utf-8"?>
<menu xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:android="http://schemas.android.com/apk/res/android">

    <item android:title="Manage Group"
        android:id="@+id/manage_group">
        <menu>
            <item android:title="Group Chat"
                android:id="@+id/group_chat"
                android:enabled="false"
                android:visible="false"
                android:icon="@android:drawable/sym_action_chat" />
            <item
                android:title="Manage Requests"
                android:id="@+id/manage_requests"
                android:icon="@drawable/ic_menu_manage" />
            <item
                android:id="@+id/nav_send"
                android:icon="@drawable/ic_menu_send"
                android:title="Share Group Code" />
        </menu>
    </item>
    <item android:title="Manage Account"
        android:id="@+id/manage_account">
        <menu >
            <item android:title="Leave Group"
                android:id="@+id/leave_group"
                android:icon="@android:drawable/ic_menu_close_clear_cancel" />
            <item
                android:id="@+id/logout"
                android:title="Log Out"
                android:icon="@android:drawable/ic_menu_directions" />
        </menu>
    </item>

</menu>
